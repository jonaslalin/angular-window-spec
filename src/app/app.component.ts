import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <h1>Angular window Spec</h1>
    <p>This page is not really used...</p>
  `
})
export class AppComponent {}
